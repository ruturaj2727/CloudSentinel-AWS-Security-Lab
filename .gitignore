# Terraform
.terraform/
terraform.tfstate
terraform.tfstate.backup
*.tfstate
*.tfstate.*
.terraform.lock.hcl

# Binaries and archives
*.zip
*.exe

# Credentials - never commit these
*.pem
*.key
credentials
